# Optional: explicit path to the local Chromium binary.
# Leave blank when `chromium` or `chromium-browser` is already on the system path.
CHROMIUM_PATH=

# Optional local listener port. The service always binds to 127.0.0.1.
PORT=3000



